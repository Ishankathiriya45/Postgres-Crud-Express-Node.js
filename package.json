{
  "name": "mysql_crud",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/server.js",
    "migrate-local:all": "sequelize db:migrate --env local",
    "migrate-test:all": "sequelize db:migrate --env test",
    "migrate-prod:all": "sequelize db:migrate --env production",
    "seed-local": "sequelize db:seed:all --env local",
    "seed-test": "sequelize db:seed:all --env test",
    "seed-prod": "sequelize db:seed:all --env production"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cli-color": "^2.0.4",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "fs": "^0.0.1-security",
    "multer": "^2.0.2",
    "nodemon": "^3.1.10",
    "path": "^0.12.7",
    "pg": "^8.16.3",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.7"
  },
  "devDependencies": {
    "sequelize-cli": "^6.6.3"
  }
}
